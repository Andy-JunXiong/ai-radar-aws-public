# Runtime Data Directory

The application may create local runtime files under this directory.
Runtime data, uploads, workspace content, model debug payloads, reviews, and
personal context must not be committed.

Tests should use temporary directories or synthetic fixtures.
